// AdminPanel intentionally disabled.
// The site no longer exposes on-site editing controls. Export a noop
// component to satisfy any remaining imports.
const AdminPanel: React.FC = () => null;

export default AdminPanel;
